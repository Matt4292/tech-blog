<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
    <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css"
  integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <header class="navbar navbar-expand-md" id="headerId">
      <a href="/" class="navbar-item nav-link" id="navItemHover">Home</a>
      <ul class="nav">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon">
            <i class="fas fa-bars"></i>
          </span>
        </button>
        <nav class="navbar mt-2 collapse navbar-collapse nav-list-container" id="navbarSupportedContent" id="nav">
          {{#if loggedIn}}
            <div class="nav-list">
              <li class="nav-item">
                <a href="/dashboard" class="navbar-item is-hoverable pt-1 nav-link" id="navItemHover">Dashboard</a>
              </li>
              <li class="nav-item">
                <a href="/dashboard/new/" class="navbar-item is-hoverable pt-1 mr-4 nav-link" id="navItemHover">Create Post</a>
              </li>
              <li class="nav-item">
                <button id="logout" class="button is-danger">Logout</button>
              </li>
            </div>
          {{else}}
            <div class="nav-list">
              <li class="nav-item nav-link">
                <a href="/login" class="button is-success">Login</a>                
              </li>
            </div>
          {{/if}}
        </nav>
      </ul>
    </header>

    <main>
      {{{ body }}}
    </main>

  {{#if loggedIn}}
    <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>